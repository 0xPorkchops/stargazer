{
  "name": "stargazer-root",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix client && npm install --prefix server",
    "build:all": "npm run build:css --prefix client && npm run build --prefix client && npm run build --prefix server",
    "start:all": "npm run start --prefix server"
  }
}
