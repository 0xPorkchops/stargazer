{
    "version": 2,
    "builds": [
      {
        "src": "src/*.ts",
        "use": "@vercel/node",
        "config": {
          "distDir": "build"
        }
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "src/server.ts"
      }
    ]
  }