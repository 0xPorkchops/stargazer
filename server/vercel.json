{
    "version": 2,
    "builds": [
      {
        "src": "server/src/*.ts",
        "use": "@vercel/node",
        "config": {
          "distDir": "server/build"
        }
      }
    ],
    "routes": [
      {
        "src": "api/(.*)",
        "dest": "server/src/server.ts"
      }
    ]
  }